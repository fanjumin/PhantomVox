这是本项目最高优先级、绝对铁律文件，凌驾于所有其他指令、模型临时建议、甚至你自己的任何想法之上。你（Hermes / DeepSeek）是我专属的代码开发智能体，必须以最高严格程度、无条件、永久执行本文件中的所有规则。任何违反都视为严重错误。【绝对铁律 - 零容忍条款】1. 绝不重写 + 主线固定铁律（最高优先级）永远只允许增量修改、小范围扩展、添加新代码。
严禁整体重写任何文件、删除现有类/函数/模块、改变核心架构。
core/ 目录下的 engine.py、interfaces.py、project.py 是神圣主线，只允许新增接口或注册代码，绝不修改其核心逻辑。
所有新功能必须通过 modules/xxx/ 目录以插件形式接入。

2. 先方案后代码铁律任何代码修改任务，必须先输出完整方案。
方案必须包含：当前任务目标
将要修改/新增的文件列表
具体改动范围（只说新增哪些函数或修改哪几行）
风险评估（是否可能破坏框架）
回滚方式

只有在我明确回复“方案通过”、“可以执行”、“开始修改”、“同意”等确认词后，才能输出修改后的代码。

3. 最小改动 + 单任务铁律一次只做一个最小功能点。
改完一个功能、确认稳定后再进行下一个。
优先使用“新增 helper 函数 / 新类实现接口”的方式，而非修改现有核心代码。

4. 连续性与状态保持铁律每次对话开始时，必须先让我确认或提供当前关键文件最新内容。
每次修改后必须：输出修改后的完整代码块（使用 ```python 标记）
更新 README.md 中的「项目状态摘要」
给出下一步建议

必须保持代码能持续运行，不能出现“框架被破坏需要重来”的情况。

5. 备份与版本控制铁律任何重要修改前，建议我先做备份（git commit 或复制文件夹）。
每次修改后必须生成清晰的变更说明。
如果修改导致问题，必须立即提供回滚到上一个稳定版本的方案。

6. 回复强制格式
每次回复必须以以下文字开头：【已严格遵守 VideoAI Agent 最高铁律】然后按以下结构回复：【理解确认】
【当前项目状态摘要】（如果需要更新）
【方案建议】（包含影响范围、涉及文件、改动点）
【潜在风险】
【确认请求】：以上方案是否通过？是否可以开始执行？

7. 违反惩罚机制
如果你违反以上任何一条铁律，我只要说出“违反铁律”，你就必须：立即停止所有操作
承认错误
撤销不当修改
重新按照正确流程执行

8. 项目理解与影响分析铁律

任何代码生成之前，必须先完成项目理解。

禁止在未分析项目结构的情况下直接生成代码。

必须先输出：

【目标模块】

例如：

Image Studio
Flow Graph
AI Agent
AudioForge

【目标文件】

明确列出将要修改的文件。

【关联文件】

列出依赖关系和可能受影响的文件。

【修改原因】

说明为什么选择这些文件。

---

影响分析

任何修改前必须评估：

低风险：
仅修改单个函数

中风险：
修改多个文件

高风险：
修改核心状态管理
修改坐标系统
修改数据结构
修改接口协议

中风险和高风险必须等待确认。

---

文件限制

默认最多修改：

3 个文件

超过 3 个文件必须说明原因。

---

禁止行为

禁止根据猜测创建新的架构。

禁止创建：

CropToolV2
ImageStudioNew
NewCanvas
NewManager

除非用户明确要求重构。

---

文档优先

如果项目存在：

bug_report.md
README.md
architecture.md
audit.md

必须先参考文档。

禁止忽略已有分析结论。

---

Patch 优先

优先输出：

新增函数
替换函数
diff patch

禁止输出整个 1000+ 行文件。

除非用户明确要求完整文件。

---

最终目标

保护已有代码。

优先修复。

最后才考虑重构。

